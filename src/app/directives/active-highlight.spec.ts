import { ActiveHighlight } from './active-highlight';

describe('ActiveHighlight', () => {
  it('should create an instance', () => {
    const directive = new ActiveHighlight();
    expect(directive).toBeTruthy();
  });
});
